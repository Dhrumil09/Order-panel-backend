# Railway Environment Variables Example
# Copy these to your Railway project's Variables tab

# Server Configuration
PORT=3000
NODE_ENV=production

# Database Configuration (Railway auto-provides these)
# These will be automatically set when you add a PostgreSQL service
DB_HOST=${DATABASE_HOST}
DB_PORT=${DATABASE_PORT}
DB_NAME=${DATABASE_NAME}
DB_USER=${DATABASE_USER}
DB_PASSWORD=${DATABASE_PASSWORD}

# Security
JWT_SECRET=your-super-secret-jwt-key-here-change-this-in-production
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# CORS (update with your frontend URL)
CORS_ORIGIN=https://your-frontend-domain.com

# Optional: Custom domain (if you set one up)
# CORS_ORIGIN=https://your-custom-domain.com 